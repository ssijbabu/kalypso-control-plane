apiVersion: scheduler.kalypso.io/v1alpha1
kind: SchedulingPolicy
metadata:
  name: observability-policy-local
spec:
  deploymentTargetSelector:
    workspace: platform-team
    labelSelector:
      matchLabels:
        stack: observability
  clusterTypeSelector:
    labelSelector: 
      matchLabels:
        location: local